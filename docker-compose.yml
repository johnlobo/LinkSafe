services:
  linksafe:
    image: ghcr.io/johnlobo/linksafe:latest
    container_name: linksafe
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - GOOGLE_GENAI_API_KEY=${GOOGLE_GENAI_API_KEY}
